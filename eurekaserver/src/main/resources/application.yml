server:
  port: 8761
eureka:
  client:
    register-with-eureka: false #表示是否把自己也注册到服务中心
    fetch-registry: false #表示是否同步其他注册中心的数据
    service-url: #访问注册中心的路径
      defaultZone: http://localhost:8761/eureka/